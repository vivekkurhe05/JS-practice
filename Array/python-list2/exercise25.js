/**
 * Write a Python program to select an item randomly from a list.
 */

let color_list = ['Red', 'Blue', 'Green', 'White', 'Black']
console.log(color_list[Math.floor(Math.random()*color_list.length)])