/**
 * Write a Python program to check whether a string starts with specified characters.
 */

function starts_with(str) {
    return str.indexOf("w3") === 0;
}

console.log(starts_with("w3resource"))